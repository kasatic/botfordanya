# Архитектура системы миграций

## 🏗️ Структура компонентов

```
┌─────────────────────────────────────────────────────────────┐
│                      Application Start                       │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    Database.init()                           │
│  • Создаёт соединение с SQLite                              │
│  • Устанавливает row_factory                                │
│  • Вызывает migrate() автоматически                         │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    Database.migrate()                        │
│  • Создаёт MigrationManager                                 │
│  • Получает список миграций из migrations/__init__.py       │
│  • Вызывает migrate_to_latest()                             │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│              MigrationManager.migrate_to_latest()            │
│  1. Получает текущую версию из schema_version               │
│  2. Фильтрует миграции (только новые)                      │
│  3. Сортирует по версии                                     │
│  4. Применяет каждую миграцию последовательно               │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│              MigrationManager.apply_migration()              │
│  1. Начинает транзакцию                                     │
│  2. Вызывает upgrade(conn)                                  │
│  3. Записывает версию в schema_version                      │
│  4. Коммитит транзакцию                                     │
│  5. При ошибке - откатывает транзакцию                      │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                  migration_XXX.upgrade()                     │
│  • Выполняет SQL команды                                    │
│  • Создаёт таблицы, индексы                                │
│  • Изменяет данные                                          │
└─────────────────────────────────────────────────────────────┘
```

## 📁 Структура файлов

```
src/database/
├── connection.py                    # Database класс
├── migrations_manager.py            # MigrationManager класс
└── migrations/                      # Папка с миграциями
    ├── __init__.py                  # Реестр миграций (MIGRATIONS)
    ├── migration_001_*.py           # Миграция 001
    ├── migration_002_*.py           # Миграция 002
    ├── migration_XXX_*.py           # Миграция XXX
    ├── README.md                    # Документация
    ├── QUICK_START.md               # Быстрый старт
    ├── ARCHITECTURE.md              # Этот файл
    └── migration_002_example.py.template  # Шаблон
```

## 🔄 Поток данных

### При первом запуске (версия = 0)

```
1. Database.init()
   └─> Database.migrate()
       └─> MigrationManager.migrate_to_latest()
           ├─> get_current_version() → 0
           ├─> Находит миграции: [1, 2, 3]
           └─> Применяет все миграции:
               ├─> apply_migration(1) ✅
               ├─> apply_migration(2) ✅
               └─> apply_migration(3) ✅

Результат: версия = 3
```

### При повторном запуске (версия = 3)

```
1. Database.init()
   └─> Database.migrate()
       └─> MigrationManager.migrate_to_latest()
           ├─> get_current_version() → 3
           ├─> Находит миграции: [1, 2, 3]
           ├─> Фильтрует: [] (все уже применены)
           └─> "Database schema is up to date" ✅

Результат: версия = 3 (без изменений)
```

### При добавлении новой миграции (версия = 3, добавлена миграция 4)

```
1. Database.init()
   └─> Database.migrate()
       └─> MigrationManager.migrate_to_latest()
           ├─> get_current_version() → 3
           ├─> Находит миграции: [1, 2, 3, 4]
           ├─> Фильтрует: [4] (только новая)
           └─> Применяет:
               └─> apply_migration(4) ✅

Результат: версия = 4
```

## 🗄️ Таблица schema_version

### Структура

```sql
CREATE TABLE schema_version (
    version INTEGER PRIMARY KEY,      -- Номер версии
    applied_at TEXT NOT NULL,         -- Дата и время применения
    description TEXT                  -- Описание миграции
);
```

### Пример данных

```
┌─────────┬─────────────────────┬──────────────────────────┐
│ version │ applied_at          │ description              │
├─────────┼─────────────────────┼──────────────────────────┤
│ 1       │ 2024-01-15 10:30:00 │ Initial database schema  │
│ 2       │ 2024-01-16 14:20:00 │ Add user roles table     │
│ 3       │ 2024-01-17 09:15:00 │ Add action logs          │
└─────────┴─────────────────────┴──────────────────────────┘
```

## 🔐 Транзакции

### Успешная миграция

```
BEGIN TRANSACTION
  ├─> CREATE TABLE ...
  ├─> CREATE INDEX ...
  ├─> INSERT INTO schema_version ...
  └─> COMMIT ✅
```

### Неудачная миграция

```
BEGIN TRANSACTION
  ├─> CREATE TABLE ... ✅
  ├─> CREATE INDEX ... ❌ ERROR!
  └─> ROLLBACK ⏪
  
Результат: все изменения отменены
```

## 📊 Диаграмма классов

```
┌─────────────────────────────────────┐
│           Database                  │
├─────────────────────────────────────┤
│ - db_path: str                      │
│ - _conn: Connection                 │
│ - _lock: Lock                       │
├─────────────────────────────────────┤
│ + init()                            │
│ + close()                           │
│ + migrate()                         │◄─────┐
│ + connection()                      │      │
│ + init_schema() [DEPRECATED]        │      │
└─────────────────────────────────────┘      │
                                              │
                                              │ использует
                                              │
┌─────────────────────────────────────┐      │
│       MigrationManager              │      │
├─────────────────────────────────────┤      │
│ - conn: Connection                  │      │
├─────────────────────────────────────┤      │
│ + init_schema_version_table()       │      │
│ + get_current_version()             │      │
│ + apply_migration()                 │◄─────┘
│ + migrate_to_latest()               │
│ + rollback_migration()              │
└─────────────────────────────────────┘
                │
                │ применяет
                ▼
┌─────────────────────────────────────┐
│      Migration Module               │
├─────────────────────────────────────┤
│ + upgrade(conn)                     │
│ + downgrade(conn)                   │
└─────────────────────────────────────┘
```

## 🎯 Принципы проектирования

### 1. Single Responsibility (Единственная ответственность)
- `Database` - управление соединением
- `MigrationManager` - управление миграциями
- `migration_XXX` - конкретная миграция

### 2. Open/Closed (Открыт для расширения, закрыт для изменения)
- Добавление новой миграции не требует изменения существующего кода
- Просто создайте новый файл и зарегистрируйте его

### 3. Dependency Inversion (Инверсия зависимостей)
- `MigrationManager` зависит от абстракции (Connection)
- Миграции зависят от абстракции (Connection)

### 4. Interface Segregation (Разделение интерфейса)
- Каждая миграция имеет простой интерфейс: `upgrade()` и `downgrade()`

## 🔍 Последовательность вызовов

### Полная последовательность при старте приложения

```
1. main()
   └─> Bot.setup()
       └─> ContainerFactory.create_configured_container()
           └─> DatabaseFactory.create()
               └─> Database.__init__()
                   └─> Database.init()
                       ├─> aiosqlite.connect()
                       └─> Database.migrate()
                           ├─> MigrationManager.__init__()
                           ├─> get_migrations()
                           └─> MigrationManager.migrate_to_latest()
                               ├─> init_schema_version_table()
                               ├─> get_current_version()
                               └─> apply_migration() (для каждой новой миграции)
                                   ├─> upgrade(conn)
                                   ├─> INSERT INTO schema_version
                                   └─> commit()
```

## 🛠️ Расширяемость

### Добавление новой миграции

```python
# 1. Создайте файл
src/database/migrations/migration_004_add_feature.py

# 2. Реализуйте функции
async def upgrade(conn): ...
async def downgrade(conn): ...

# 3. Зарегистрируйте в __init__.py
from .migration_004_add_feature import upgrade as m004_upgrade, downgrade as m004_downgrade

MIGRATIONS = [
    ...,
    (4, m004_upgrade, m004_downgrade, "Add feature"),
]
```

### Добавление новой функциональности в MigrationManager

```python
class MigrationManager:
    # Существующие методы...
    
    async def get_migration_history(self) -> List[Dict]:
        """Получает историю всех применённых миграций."""
        cursor = await self.conn.execute(
            "SELECT * FROM schema_version ORDER BY version"
        )
        return await cursor.fetchall()
```

## 📈 Производительность

### Оптимизации

1. **Ленивая загрузка**: Миграции импортируются только при необходимости
2. **Транзакции**: Все изменения в одной транзакции
3. **Индексы**: Используйте индексы для больших таблиц
4. **Батчинг**: Для больших изменений данных используйте батчи

### Пример оптимизированной миграции

```python
async def upgrade(conn: aiosqlite.Connection) -> None:
    """Миграция с батчингом для больших данных."""
    # Создаём таблицу
    await conn.execute("CREATE TABLE ...")
    
    # Обновляем данные батчами
    batch_size = 1000
    offset = 0
    
    while True:
        cursor = await conn.execute(
            f"SELECT * FROM old_table LIMIT {batch_size} OFFSET {offset}"
        )
        rows = await cursor.fetchall()
        
        if not rows:
            break
        
        # Обрабатываем батч
        for row in rows:
            await conn.execute("INSERT INTO new_table ...", row)
        
        offset += batch_size
```

## 🔒 Безопасность

### Защита от SQL-инъекций

```python
# ✅ Безопасно (параметризованный запрос)
await conn.execute(
    "INSERT INTO users (name) VALUES (?)",
    (user_name,)
)

# ❌ Небезопасно (конкатенация строк)
await conn.execute(
    f"INSERT INTO users (name) VALUES ('{user_name}')"
)
```

### Защита от частичного применения

- Все изменения в транзакции
- При ошибке - автоматический откат
- Версия записывается только после успешного применения

## 📝 Логирование

### Уровни логирования

```python
logger.debug()  # Детальная информация (версия схемы)
logger.info()   # Основные события (применение миграций)
logger.warning()  # Предупреждения (deprecated методы)
logger.error()  # Ошибки (неудачные миграции)
```

### Пример логов

```
DEBUG - Current schema version: 2
INFO  - 📦 Found 1 pending migration(s)
INFO  - 📦 Applying migration 003: Add user roles
INFO  - ✅ Migration 003 applied successfully
INFO  - ✅ All migrations applied successfully
```

## 🧪 Тестирование

### Юнит-тесты

```python
async def test_migration_manager():
    db = Database(":memory:")
    await db.init()
    
    async with db.connection() as conn:
        manager = MigrationManager(conn)
        version = await manager.get_current_version()
        assert version == 1
```

### Интеграционные тесты

```python
async def test_full_migration_flow():
    db = Database(":memory:")
    await db.init()  # Миграции применяются автоматически
    
    # Проверяем, что таблицы созданы
    async with db.connection() as conn:
        cursor = await conn.execute(
            "SELECT name FROM sqlite_master WHERE type='table'"
        )
        tables = [row[0] for row in await cursor.fetchall()]
        assert 'spam_records' in tables
```

---

**Документ:** Архитектура системы миграций  
**Версия:** 1.0  
**Статус:** ✅ Актуально
