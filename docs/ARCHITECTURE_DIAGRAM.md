# 📐 Диаграммы архитектуры

## 🏗️ Общая структура

```
┌─────────────────────────────────────────────────────────────────┐
│                         TELEGRAM BOT                             │
│                      (External Interface)                        │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                         HANDLERS                                 │
│                    (Presentation Layer)                          │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ SpamHandlers │  │ MenuHandlers │  │ DotaHandlers │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                  │
│  Responsibilities:                                               │
│  • Validate input                                                │
│  • Call services                                                 │
│  • Format responses                                              │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                         SERVICES                                 │
│                    (Application Layer)                           │
│                                                                  │
│  ┌───────────────┐  ┌────────────┐  ┌─────────────┐            │
│  │ SpamDetector  │  │ BanService │  │ DotaService │            │
│  └───────────────┘  └────────────┘  └─────────────┘            │
│                                                                  │
│  Responsibilities:                                               │
│  • Business logic                                                │
│  • Orchestrate operations                                        │
│  • Apply business rules                                          │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                      REPOSITORIES                                │
│                  (Infrastructure Layer)                          │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ SpamRepo     │  │ ViolationRepo│  │ SteamRepo    │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                  │
│  Responsibilities:                                               │
│  • Database access                                               │
│  • Map Row → Domain Models                                       │
│  • External API calls                                            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                         DOMAIN                                   │
│                      (Domain Layer)                              │
│                                                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │ BanInfo  │  │ User     │  │ SpamType │  │ BanLevel │        │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘        │
│                                                                  │
│  Responsibilities:                                               │
│  • Business entities                                             │
│  • Business rules                                                │
│  • Domain exceptions                                             │
│  • NO dependencies                                               │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                           CORE                                   │
│                     (Configuration)                              │
│                                                                  │
│  ┌──────────┐  ┌───────────┐  ┌──────────┐                     │
│  │ Config   │  │ Constants │  │ Types    │                     │
│  └──────────┘  └───────────┘  └──────────┘                     │
│                                                                  │
│  Used by all layers                                              │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Поток данных: Обработка спама

```
┌─────────────┐
│   Telegram  │
│   Message   │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│ 1. HANDLER (spam_refactored.py)                                 │
│                                                                  │
│  async def handle_sticker(update, context):                     │
│      user_id = UserId(update.effective_user.id)                 │
│      chat_id = ChatId(update.effective_chat.id)                 │
│                                                                  │
│      # Валидация                                                 │
│      if not is_group_chat: return                                │
│      if is_admin: return                                         │
│                                                                  │
│      # Вызов сервиса ↓                                           │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. SERVICE (spam_detector_refactored.py)                        │
│                                                                  │
│  async def check(user_id, chat_id, spam_type):                  │
│      # Получить настройки чата                                   │
│      settings = await settings_repo.get(chat_id)                │
│      limit, window = settings.get_limits(spam_type)             │
│                                                                  │
│      # Проверить белый список                                    │
│      if await whitelist_repo.is_whitelisted(...): return OK     │
│                                                                  │
│      # Записать и посчитать ↓                                    │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. REPOSITORY (repositories_refactored.py)                      │
│                                                                  │
│  async def add_and_count_recent(...):                           │
│      # SQL: INSERT spam record                                   │
│      # SQL: COUNT recent records                                 │
│      return count                                                │
│                                                                  │
│      # Возвращает int ↑                                          │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. SERVICE (spam_detector_refactored.py)                        │
│                                                                  │
│      # Создать доменную модель                                   │
│      return SpamCheckResult(                                     │
│          is_spam=(count >= limit),                               │
│          is_warning=(count == limit - 1),                        │
│          count=count,                                            │
│          limit=limit,                                            │
│          spam_type=spam_type                                     │
│      )                                                           │
│                                                                  │
│      # Возвращает SpamCheckResult ↑                              │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. HANDLER (spam_refactored.py)                                 │
│                                                                  │
│      result = await spam_detector.check(...)                    │
│                                                                  │
│      if result.is_warning:                                       │
│          await send_warning_message()                            │
│          return                                                  │
│                                                                  │
│      if result.is_spam:                                          │
│          await delete_message()                                  │
│          success, count, minutes = await ban_service.apply_ban() │
│          await send_ban_notification()                           │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│ 6. SERVICE (ban_service_refactored.py)                          │
│                                                                  │
│  async def apply_ban(context, chat_id, user_id, ...):           │
│      # Получить текущую информацию                               │
│      ban_info = await violation_repo.get_ban_info(...)          │
│                                                                  │
│      # Определить уровень бана                                   │
│      level = BanLevel.from_violation_count(ban_info.count + 1)  │
│      minutes = level.duration_minutes                            │
│                                                                  │
│      # Записать нарушение ↓                                      │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│ 7. REPOSITORY (repositories_refactored.py)                      │
│                                                                  │
│  async def increment_and_get(...):                              │
│      # SQL: UPDATE violations SET count = count + 1             │
│      # SQL: UPDATE banned_until                                  │
│      return new_count                                            │
│                                                                  │
│  async def record_ban(...):                                     │
│      # SQL: INSERT INTO ban_stats                                │
│                                                                  │
│      # Возвращает int ↑                                          │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│ 8. SERVICE (ban_service_refactored.py)                          │
│                                                                  │
│      # Применить ограничения через Telegram API                  │
│      await context.bot.restrict_chat_member(...)                │
│                                                                  │
│      return (success, violation_count, ban_minutes)              │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│ 9. HANDLER (spam_refactored.py)                                 │
│                                                                  │
│      # Форматировать и отправить уведомление                     │
│      text = Messages.ban_notification(...)                       │
│      await context.bot.send_message(chat_id, text)              │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔗 Граф зависимостей

```
                    ┌──────────┐
                    │ Telegram │
                    │   Bot    │
                    └────┬─────┘
                         │
                         ▼
              ┌──────────────────────┐
              │      Handlers        │
              │  (Presentation)      │
              └──────────┬───────────┘
                         │
         ┌───────────────┼───────────────┐
         │               │               │
         ▼               ▼               ▼
    ┌────────┐     ┌─────────┐    ┌──────────┐
    │ Spam   │     │  Ban    │    │  Admin   │
    │Detector│     │ Service │    │ Service  │
    └────┬───┘     └────┬────┘    └────┬─────┘
         │              │              │
         │              │              │
         └──────┬───────┴──────┬───────┘
                │              │
                ▼              ▼
         ┌─────────────────────────┐
         │     Repositories        │
         │   (Infrastructure)      │
         └────────┬────────────────┘
                  │
         ┌────────┼────────┐
         │        │        │
         ▼        ▼        ▼
    ┌────────┐ ┌────────┐ ┌────────┐
    │ Spam   │ │Violation│ │Settings│
    │  Repo  │ │  Repo  │ │  Repo  │
    └────┬───┘ └────┬───┘ └────┬───┘
         │          │          │
         └──────────┼──────────┘
                    │
                    ▼
         ┌──────────────────────┐
         │       Domain         │
         │   (Business Logic)   │
         └──────────────────────┘
         
         ┌──────────────────────┐
         │        Core          │
         │   (Configuration)    │
         └──────────────────────┘
```

---

## 📦 Модульная структура

```
src/
│
├── domain/                    # ❌ NO dependencies
│   ├── models.py             # Entities & Value Objects
│   ├── enums.py              # Business enums
│   └── exceptions.py         # Domain exceptions
│
├── core/                      # ❌ NO dependencies
│   ├── config.py             # Configuration
│   ├── constants.py          # Magic numbers
│   └── types.py              # Type aliases
│
├── database/                  # ✅ Depends on: domain, core
│   ├── connection.py         # DB connection
│   ├── repositories.py       # Old (deprecated)
│   └── repositories_refactored.py  # New (domain models)
│
├── services/                  # ✅ Depends on: domain, core, database
│   ├── spam_detector.py      # Old (deprecated)
│   ├── spam_detector_refactored.py  # New
│   ├── ban_service.py        # Old (deprecated)
│   └── ban_service_refactored.py    # New
│
├── handlers/                  # ✅ Depends on: all layers
│   ├── spam.py               # Old (fat controllers)
│   └── spam_refactored.py    # New (thin controllers)
│
└── ui/                        # ✅ Depends on: domain
    ├── keyboards.py          # Telegram keyboards
    └── messages.py           # Message templates
```

---

## 🎯 Dependency Rules

### ✅ Allowed:

```
Handlers    → Services
Handlers    → Repositories (only for simple queries)
Handlers    → Domain
Handlers    → Core
Handlers    → UI

Services    → Repositories
Services    → Domain
Services    → Core

Repositories → Domain
Repositories → Core

UI          → Domain
UI          → Core
```

### ❌ Forbidden:

```
Domain      → ANY (except stdlib)
Core        → ANY (except stdlib)

Repositories → Services
Repositories → Handlers

Services    → Handlers
Services    → UI

Domain      → Services
Domain      → Repositories
Domain      → Handlers
```

---

## 🔍 Пример: Проверка зависимостей

### ✅ Правильно:

```python
# handlers/spam_refactored.py
from src.domain import SpamType, User          # ✅ OK
from src.core.types import UserId, ChatId      # ✅ OK
from src.services.spam_detector_refactored import SpamDetector  # ✅ OK
from src.ui import Messages                    # ✅ OK
```

```python
# services/spam_detector_refactored.py
from src.domain import SpamType, ChatSettings  # ✅ OK
from src.core.types import UserId, ChatId      # ✅ OK
from src.database.repositories_refactored import SpamRepository  # ✅ OK
```

```python
# database/repositories_refactored.py
from src.domain import BanInfo, SpamType       # ✅ OK
from src.core.types import UserId, ChatId      # ✅ OK
```

### ❌ Неправильно:

```python
# domain/models.py
from src.database import SpamRepository        # ❌ FORBIDDEN
from src.services import BanService            # ❌ FORBIDDEN
```

```python
# services/spam_detector.py
from src.handlers import SpamHandlers          # ❌ FORBIDDEN
from src.ui import Messages                    # ❌ FORBIDDEN (use in handlers)
```

```python
# database/repositories.py
from src.services import BanService            # ❌ FORBIDDEN
```

---

## 📊 Метрики качества архитектуры

### Coupling (Связанность):

```
Domain       → 0 dependencies  ✅ Perfect
Core         → 0 dependencies  ✅ Perfect
Repositories → 2 dependencies  ✅ Good (domain, core)
Services     → 3 dependencies  ✅ Good (domain, core, repositories)
Handlers     → 5 dependencies  ⚠️  Acceptable (all layers)
```

### Cohesion (Связность):

```
Domain       → High  ✅ (business entities)
Core         → High  ✅ (configuration)
Repositories → High  ✅ (data access)
Services     → High  ✅ (business logic)
Handlers     → Medium ⚠️  (coordination)
```

### Testability:

```
Domain       → 100% ✅ (pure functions)
Services     → 95%  ✅ (mock repositories)
Repositories → 90%  ✅ (test database)
Handlers     → 80%  ⚠️  (mock services + telegram)
```

---

## 🚀 Преимущества архитектуры

1. **Независимость от фреймворков**
   - Можно заменить Telegram на Discord без изменения бизнес-логики

2. **Тестируемость**
   - Domain: чистые unit-тесты
   - Services: моки репозиториев
   - Handlers: моки сервисов

3. **Гибкость**
   - Можно заменить SQLite на PostgreSQL
   - Можно добавить REST API параллельно с Telegram

4. **Понятность**
   - Явное разделение ответственности
   - Легко найти нужный код

5. **Масштабируемость**
   - Легко добавлять новые features
   - Легко рефакторить отдельные слои

---

**Вопросы?** См. [README_ARCHITECTURE.md](../README_ARCHITECTURE.md) или [MIGRATION_GUIDE.md](../MIGRATION_GUIDE.md)
